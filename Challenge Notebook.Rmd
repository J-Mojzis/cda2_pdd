---
title: "R Notebook"
output: html_notebook
---
```{r}
daten_challenge <- read.csv2("C:/Users/chant/Dokumente/Übung Deskriptive Statistik mit R/Dataset_Challenge.csv")
daten_challenge
```

```{r}
library(tidyverse)
library(dplyr)
library(tidyr)
```

```{r}
ggplot(daten_challenge, aes(as.Date(daten_challenge$datum,"%d.%m.%Y"), annahme)) +
  geom_jitter() +
  facet_grid(rows = vars(d1e1)) +
  geom_vline(data=daten_challenge, mapping=aes(xintercept=as.Date(daten_challenge$datum[229],"%d.%m.%Y")), color="blue", colour = 'red')
```

```{r}
group_by(daten_challenge$"rechtsform")
```

```{r}
filter(daten_challenge$rechtsform)
```

```{r}
hist(daten_challenge$rechtsform)
```

```{r}
rechtsform <- daten_challenge %>%
  group_by(rechtsform) %>%
  count()
```

```{r}

```

